#include "main.h"
#include <string.h>

/**
 * _strlen - function that returns the length of a string
 * @s: String pointer passesd to this function
 *
 * Return: returns length of the string
 *
 */
int _strlen(char *s)
{
	return (strlen(s));
}

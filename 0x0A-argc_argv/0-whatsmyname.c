#include <stdio.h>

/**
 * main -> A function that prints the name of the program
 * @argc: Number of the command lines argument
 * @argv: An array of a command listed
 * Return: 0 for success
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[argc * 0]);

	return (0);
}

#include <stdio.h>

/**
 * main - Prints the alphabet in lowercase
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char letter;

	for (low = 'a'; low <= 'z'; letter++)
		putchar(letter);

	putchar('\n,);

	return (0);
}
